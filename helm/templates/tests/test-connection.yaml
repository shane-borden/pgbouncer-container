apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google-pgbouncer.fullname" . }}-test-connection"
  labels:
    {{- include "google-pgbouncer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google-pgbouncer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
